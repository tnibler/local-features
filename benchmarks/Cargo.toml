[package]
name = "benchmarks"
version = "0.1.0"
edition = "2024"

[lib]
bench = false

[dependencies]
anyhow = "1.0.100"
itertools.workspace = true
opencv = { version = "0.96.0", optional = true, features = ["clang-runtime"] }
local-features = { workspace = true }

[dev-dependencies]

criterion.workspace = true
image.workspace = true
ndarray.workspace = true
nshare.workspace = true

[build-dependencies]
bindgen = "0.72.1"

[[bench]]
name = "benchmark"
harness = false

[features]
default = []
vulkansift = []
